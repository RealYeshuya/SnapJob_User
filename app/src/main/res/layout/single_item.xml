<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/singleItem"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/textline">

    <ImageView
        android:id="@+id/workerPic"
        android:layout_width="82dp"
        android:layout_height="82dp"
        android:layout_marginStart="20dp"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_baseline_person_24" />

    <TextView
        android:id="@+id/workerName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:fontFamily="@font/raleway_semibold"
        android:text="Name: "
        android:textColor="#000"
        android:textSize="16sp"
        app:layout_constraintStart_toEndOf="@+id/workerPic"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/workerJob"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="12dp"
        android:text="Job: "
        android:textSize="16sp"
        android:textColor="#000"
        android:fontFamily="@font/raleway_semibold"
        app:layout_constraintStart_toEndOf="@+id/workerPic"
        app:layout_constraintTop_toBottomOf="@+id/workerAvailablility" />

    <TextView
        android:id="@+id/workerAvailablility"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="12dp"
        android:text="Availability: "
        android:textSize="16sp"
        android:textColor="#000"
        android:fontFamily="@font/raleway_semibold"
        app:layout_constraintStart_toEndOf="@+id/workerPic"
        app:layout_constraintTop_toBottomOf="@+id/workerName" />

</androidx.constraintlayout.widget.ConstraintLayout>